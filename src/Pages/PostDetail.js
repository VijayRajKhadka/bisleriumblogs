import NavBar from "../Components/NavBar";
import SideBar from "../Components/SideBar";

const PostDetail = () => {
    return (
        <div>
            <NavBar />
            <SideBar />
        </div>
    );
}